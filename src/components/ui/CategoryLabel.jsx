/**
 * Styled text badge for one of the three image categories.
 * Color is derived from the label value using the Tailwind custom colors
 * (zone-seatbelt, zone-distracted, zone-safe) defined in tailwind.config.js.
 *
 * Props:
 *   label  'seatbelt' | 'distracted' | 'safe'
 */
export default function CategoryLabel({ label }) {
  return <div></div>
}
