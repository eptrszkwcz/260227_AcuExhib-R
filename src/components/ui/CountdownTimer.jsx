/**
 * Displays a live countdown (in seconds) toward some deadline.
 * Used on the Results page for auto-advance countdown and optionally
 * on any page to show remaining idle time.
 *
 * Props:
 *   remainingMs  number — milliseconds remaining
 *   label        string — e.g. "Returning to start in"
 */
export default function CountdownTimer({ remainingMs, label }) {
  return <div></div>
}
