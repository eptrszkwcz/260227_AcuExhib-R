/**
 * Displays sorting progress for one player: "X of 48 sorted".
 *
 * Props:
 *   current  number — cards sorted so far
 *   total    number — total cards assigned (48 in 1P, 24 in 2P)
 */
export default function ProgressBar({ current, total }) {
  return <div></div>
}
