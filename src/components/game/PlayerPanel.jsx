/**
 * The complete game panel for one player.
 * Contains a CardDeck and three DropZone columns (seatbelt, distracted, safe).
 * Manages its own drop zone refs for hit-testing during drag.
 *
 * Props:
 *   playerIndex  0 | 1
 */
export default function PlayerPanel({ playerIndex }) {
  return <div></div>
}
