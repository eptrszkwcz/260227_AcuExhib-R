/**
 * Renders the queue of remaining image cards for one player.
 * Shows the current top card (to be classified) prominently.
 *
 * Props:
 *   playerIndex   0 | 1
 *   onDrop        (imageId, zoneLabel) => void
 *   getDropZones  () => [{ label, rect }]
 */
export default function CardDeck({ playerIndex, onDrop, getDropZones }) {
  return <div></div>
}
