/**
 * Divides the 1920×1080 canvas into two equal 960px side-by-side panels
 * for two-player mode. Each child receives its own scrolling/overflow context.
 *
 * Usage:
 *   <SplitLayout left={<PlayerPanel playerIndex={0} />} right={<PlayerPanel playerIndex={1} />} />
 */
export default function SplitLayout({ left, right }) {
  return <div></div>
}
